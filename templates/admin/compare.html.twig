{% extends '@EasyAdmin/layout.html.twig' %}

{% block content_title %}Compare{% endblock %}

{% block main %}

    <div class="compare-container">

        <table class="table table-bordered" style='table-layout:fixed;'>
            <thead>
            <tr>
                <th>{{ entity1 }}</th>
                <th>{{ entity2 }}</th>
            </tr>
            </thead>
            <tbody>
            <tr>

                <td>{{ entity1.text  }}</td>
                <td>{{ entity2.text  }}</td>
            </tr>

            </tbody>
        </table>

{#        <div class="mt-3">}#}
{#            <a href="{{ path('admin_bug_report_index', {#}
{#                'duplicates': 1#}
{#            }) }}" class="btn btn-primary">#}
{#                Back to list#}
{#            </a>#}
{#        </div>#}
{#    </div>#}

    <style>
        .compare-container {
            max-width: 1200px;
            margin: 0 auto;
        }
        table td {
            vertical-align: middle;
        }
        .diff {
            background-color: #ffdddd;
        }
    </style>
{% endblock %}